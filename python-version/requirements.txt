# Research Agent — dependencies

# arXiv search
arxiv

# Ranking (MiniLM embeddings + numpy cosine)
sentence-transformers
numpy

# LLM (query expansion, routing, extraction, synthesis)
# LangChain is a thin wrapper only — structured output, not orchestration.
langchain
langchain-google-genai
langchain-anthropic
python-dotenv

# Schemas
pydantic

# UI
streamlit
streamlit-agraph

# Dev
black
flake8
